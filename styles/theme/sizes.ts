import { TASKBAR_HEIGHT } from "utils/constants";

const sizes = {
  taskbar: {
    height: `${TASKBAR_HEIGHT}px`,
    clock: {
      fontSize: "12px",
      width: "80px",
    },
    startButton: {
      width: "50px",
    },
  },
};

export default sizes;
